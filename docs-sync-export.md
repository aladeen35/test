# ☁️ المزامنة الأونلاين + التصدير — دليل سريع

## تفعيل المزامنة الدائمة (مجاني)
1. أنشئ حسابًا مجانيًا على **jsonbin.io**.
2. من لوحة الحساب: **API Keys** → انسخ مفتاح **X-MASTER-KEY**.
3. في التطبيق: تبويب الإعدادات ← "المزامنة الأونلاين الدائمة" ← فعّل المفتاح ← ألصق المفتاح ← اضغط **"ربط / إنشاء صندوق"**.
4. سيُنشأ صندوق تلقائيًا ويظهر لك **رمز الصندوق (Bin ID)** — بياناتك الآن تُرفع تلقائيًا بعد كل تعديل.

### المزامنة بين أجهزتك
على أي جهاز آخر: أدخل **نفس المفتاح** و**نفس رمز الصندوق** ثم اضغط **"⬇️ جلب من السحابة"**. بعدها يتزامن الجهازان تلقائيًا.

## التصدير
- **الفروع → Excel**: تبويب الفروع ← "📊 تصدير Excel" (كل البيانات: الاسم، المنطقة، الحجم، مدة الزيارة، الإحداثيات، الحالة، رابط الملاحة).
- **الخطة → PDF / صورة**: تبويب الخطة ← "📄 PDF" أو "🖼️ صورة" (جدول ملوّن بلون كل يوم).
- **اللوحة → PDF / Excel**: تبويب اللوحة ← "📄 PDF" (تقرير مصوّر ببطاقات ورسم بياني وجدول) أو "📊 Excel" (سجل تفصيلي). التقرير يحترم الفلتر المختار (هذا الأسبوع / الماضي / الكل).
