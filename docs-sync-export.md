# ☁️ المزامنة والفريق + التصدير — دليل سريع

## تفعيل المزامنة بين أجهزتك (بلا حساب — بضغطة واحدة)
1. تبويب **الإعدادات** ← "المزامنة بين أجهزتك" ← فعّل المفتاح.
2. يُنشأ صندوق سحابي مجاني تلقائيًا ويظهر لك **رمز المزامنة** (يبدأ بـ `HM`).
3. على جهازك الآخر: ألصق الرمز في خانة "لديك رمز من جهازك الأول؟" واضغط **"🔗 ربط وجلب"** — بعدها يتزامن الجهازان تلقائيًا مع كل تعديل.

> خيار متقدم: صندوق **JSONBin** خاص بمفتاح X-Master-Key من قسم "خيار متقدم" لمن يريد خصوصية أعلى.

## نظام الفريق (بلا حسابات وبلا مفاتيح)
1. تبويب **الإعدادات** ← "نظام الفريق" ← اكتب اسمك ← **"✨ إنشاء فريق جديد بضغطة واحدة"**.
2. اضغط **"↗ مشاركة رابط الدعوة"** وأرسله للأعضاء — من يفتح الرابط **ينضم تلقائيًا** وتظهر زياراته في لوحة الجميع (أو يلصق رمز `HM…` في خانة الانضمام).
3. للشركات: انسخ رمز الدعوة وضعه في `js/config.js` (`team.defaultCode`) قبل النشر، فينضم كل من يفتح التطبيق تلقائيًا.

## الذكاء الاصطناعي — يعمل فورًا
افتح تبويب **الذكاء** وأرسل رسالتك مباشرة (تسجيل دخول مجاني لمرة واحدة لدى المزوّد المجاني). اختياريًا: أضف مفتاح Anthropic الخاص بك من "إعداد الذكاء"، أو انشر `cloudflare-worker-ai-proxy.js` بمفتاح الشركة وضع عنوانه في `js/config.js`.

## التصدير
- **الفروع → Excel**: تبويب الفروع ← "📊 تصدير Excel" (كل البيانات: الاسم، المنطقة، الحجم، مدة الزيارة، الإحداثيات، الحالة، رابط الملاحة).
- **الخطة → PDF / صورة**: تبويب الخطة ← "📄 PDF" أو "🖼️ صورة" (جدول ملوّن بلون كل يوم).
- **اللوحة → PDF / Excel**: تبويب اللوحة ← "📄 PDF" (تقرير مصوّر ببطاقات ورسم بياني وجدول) أو "📊 Excel" (سجل تفصيلي يشمل أعضاء الفريق). التقرير يحترم الفلتر المختار (هذا الأسبوع / الماضي / الكل).
